{
  "name": "dbt-lineage-local",
  "displayName": "DBT Lineage (Local)",
  "version": "0.0.1",
  "engines": { "vscode": "^1.85.0" },
  "activationEvents": ["command:dbtLineage.show"],
  "main": "extension.js",
  "contributes": {
    "commands": [
      {
        "command": "dbtLineage.show",
        "title": "Show dbt Lineage (1-step)"
      }
    ]
  }
}
